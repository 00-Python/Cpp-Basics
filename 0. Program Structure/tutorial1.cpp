#include <iostream>  // This line imports the iostram Library
using namespace std; // Specifies the namespace

int main() {                      // Program Entry Point
  cout << "Hello World!" << endl; // Print Hello World! To Console

  return 0; // (it is not neccesary in the entry point) functions must return a value of the indicated function type.
}